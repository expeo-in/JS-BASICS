let name = "siva"; //global variable but not attached to window
const age = 50; //global variable but not attached to window
var weight = 100; //global variable and attached to window


console.log(name);
//console.log(product);

function displayProduct() {
    console.log(product);

}

// display();

